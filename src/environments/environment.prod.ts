export const environment = {
  production: true,
  baseURL : "https://frontend-test-api.aircall.io/",
  authentication : "auth/login",
  calls : "calls?",
  refresher : "auth/refresh-token",
  callById : "calls/"
};
